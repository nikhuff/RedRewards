<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- RED application theme. -->
    <style name="REDAppTheme" parent="Theme.AppCompat.Light.NoActionBar">s
        <item name="colorPrimary">@color/altColorPrimary</item>
        <item name="colorPrimaryDark">@color/altColorPrimaryDark</item>
        <item name="android:colorAccent">@color/altColorAccent</item>
     <!--   <item name="colorAccent">@color/altColorAccent</item> -->
    </style>

    <style name="REDAppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="REDAppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="REDAppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="tw__TweetDarkWithActionsStyle" parent="tw__TweetDarkStyle">
        <item name="tw__tweet_actions_enabled">true</item>
    </style>

</resources>
